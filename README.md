# Background

Minimal React app prepared for GitHub Pages deployment.

Live site:

- `https://viksua.github.io/background/`

The app uses hash-based routing so the example pages also work on GitHub Pages.
